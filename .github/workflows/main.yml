name: Linting

on: [push]

jobs:
  linting:
    runs-on: ubuntu-latest
    steps:
    - name: Linting
      uses: snakemake/snakemake-github-action@latest
      with:
        directory: .test
        snakefile: workflow/Snakefile
        args: "--lint"
